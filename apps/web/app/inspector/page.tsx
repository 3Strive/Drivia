'use client';

import React from 'react';
import AppLayout from '../components/template/general-layout';

const Inspector = () => {
  return (
    <AppLayout>
      Get Verified and Inspect Cars as an Inspector(charges Apply), dealers earn
      the inspected badge after you inspect this drives more sales
    </AppLayout>
  );
};

export default Inspector;
